

function Dashboard() {
  return (
    <div>Dashboard</div>
  )
}

export default Dashboard

// stats for interactions with posts(likes,comments),
// new users(account created),visitors, total views on all posts,
